// Minimal stub so the generated Office Script typechecks in this repo.
// The real ExcelScript types exist only inside the M365 Office Scripts editor.
declare namespace ExcelScript {
  interface Workbook {}
}
